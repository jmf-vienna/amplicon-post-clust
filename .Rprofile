library(conflicted)

if (interactive()) {
  require(targets)

  require(cli)
  require(dplyr)
  require(fs)
  require(purrr)
  require(readr)
  require(rlang)
  require(stringr)
  require(tibble)
  require(tidyr)
  require(vctrs)
}
